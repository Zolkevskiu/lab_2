#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>
#include <locale.h>

int main(void)
{
    int a, b, c;
    float p, area, ha, hb, hc;

    setlocale(LC_ALL, "ukr");

    printf("a = ");
    scanf("%i", &a);
    printf("b = ");
    scanf("%i", &b);
    printf("c = ");
    scanf("%i", &c);

    // ????????? ?? ????????? ??????????
    if ((a >= b + c) || (b >= a + c) || (c >= a + b) || (a <= 0) || (b <= 0) || (c <= 0)) {
        printf("????????? ?? ?????!\n");
    }
    else {
        p = (a + b + c) / 2.0;

        // ?????????? ????? ??????????
        area = sqrtf(p * (p - a) * (p - b) * (p - c));

        // ?????????? ?????
        ha = 2 * area / a;
        hb = 2 * area / b;
        hc = 2 * area / c;

        printf("Area of ??the triangle/????? ?????????? = %8.3f\n", area);
        printf("Height to the side/?????? ?? ??????? a = %8.3f\n", ha);
        printf("Height to the side/?????? ?? ??????? b = %8.3f\n", hb);
        printf("Height to the side/?????? ?? ??????? c = %8.3f\n", hc);
    }

    return 0;
}
